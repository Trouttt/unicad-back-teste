export enum COMMENT_ERRORS {
  commentDoesntExistWithThisId = 'Não existe comentário com esse id',
  onlyCommentOwnerCanEdit = 'Apenas o dono do comentário pode editar ele',
  onlyCommentOwnerOrPostOwnerCanDelete = 'Apenas o dono do comentário ou o dono da postagem podem excluir ele',
}
