{
  "buildCommand": "yarn build",
  "devCommand": "yarn start:dev",
  "installCommand": "yarn",
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/deliveries",
      "methods": ["POST", "GET"]
    }
  ]
}
